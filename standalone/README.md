# Standalone files

Small standalone examples for syntax highlighting coverage.

These examples are intentionally not full projects. Some duplicate languages
that also have project folders so this directory can act as a compact coverage
set for the syntaxes supported by `two-face`.

`twoface-syntax-map.tsv` records explicit grammar mappings for files where
extension-based detection is ambiguous or unavailable.
